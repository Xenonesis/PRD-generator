                    <span>{audit.isComplete ? 'Edit Section' : 'Fix Field'}</span>
                    <ArrowRight className="w-3 h-3" />
                  </button>
                </div>
              </div>
            ))}
        </div>

      </div>
    </div>
  );
};
